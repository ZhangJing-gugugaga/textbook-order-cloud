# 教材征订系统 · 服务端 PRD（textbook-order-server）

## 概念版 PRD（≤ 200 字）

核心用户：教材室管理员、学院秘书、任课教师、学生、供货商（5 角色），及小程序/Web 两类前端。
要解决的一件事：为征订全流程提供带 RBAC 数据隔离、两级审查、通知确认闭环与学期数据双缓冲切换的统一后端服务。
产品形态：Spring Boot 3 单体服务（REST API + 定时任务 + Excel 导入导出）。
最小可用版功能（≤ 3 条）：
  1. RBAC 五表 + 学院级数据隔离 + 供货商物理隔离只读接口；
  2. 征订窗口引擎（自动开关/无限次延长/提前截止+自动通知）与学期双缓冲原子切换；
  3. 教师填报两级审查、学生选购、通知确认闭环、Excel 异步导入导出。
本版本不做：支付、微服务拆分、消息队列、教学班模型、AI 功能。
商业模式：校内免费。
技术前提：MySQL 8、moonzj.com（已备案 HTTPS）、账号超管分发制、小程序 openid 静默收集。
> 各模块的异常分支、状态机、字段校验规则与错误文案详见落地版。

---

## 落地版 PRD

以下按概述 / 功能列表 / 服务结构 / 功能说明组织；每个模块均定义异常分支、状态机、字段校验规则与错误文案，全局规范见模块 8。

### 一、概述

**文档信息**

| 版本号 | 日期 | 作者 |
|--------|------|------|
| V 1.0.0 | 2026-09-17 | ZhangJing-gugugaga |

#### 1. 需求背景

- **目标用户现状**：教材征订依赖线下流程，无统一后端；前端（Web/小程序）需要一套支撑 5 角色隔离协作的服务端。
- **核心痛点**：无权限体系、无数据隔离、无通知确认追踪、学期更替数据切换要停服。
- **当前问题汇总**：

| 问题点 | 问题描述 | 当前影响 |
|--------|----------|----------|
| 无权限体系 | 5 角色权限靠人工约定 | 越权与泄露风险 |
| 无隔离 | 秘书/教师/学生/供货商数据范围无边界 | 隐私风险 |
| 无通知闭环 | 发送与确认无记录 | 漏订无法追责 |
| 学期切换 | 新学期数据更替需停服或人工换库 | 业务中断 |

> 上述痛点决定了各模块必须内建异常分支与数据校验；状态机与错误文案统一在「四、功能说明」与模块 8 汇总。

#### 2. 需求目标

- **业务目标**：
  1. 支撑 5 角色 × 万级学生规模的征订业务，数据隔离零越权；
  2. 学期数据双缓冲切换零停机、零感（在线用户无感）；
  3. 通知发送/确认全链路落库可导出。
- **成功标准**：

| 指标 | 当前状态 | 目标状态 |
|------|----------|----------|
| 越权用例 | 无度量 | 越权矩阵 100% 拒绝 |
| 学期切换 | 停服人工 | 单事务原子切换，业务无感 |
| 通知追踪 | 无 | 每轮发送时间/状态/确认时间完整落库 |
| 万行导入 | 前端解析易超时 | 异步批次，进度可查 |

> 为达成目标，各模块的异常分支处理、字段校验规则与错误文案在第四章逐项定义。

#### 3. 需求范围

- **包含**：认证与账号、RBAC 与数据隔离、组织与学期（含窗口引擎、双缓冲）、教材库/课程任课、教师征订两级审查、学生选购、异动两级审批、通知确认闭环、Excel 导入中心、供货商只读接口、导出中心、审计日志。
- **不包含**：支付；微服务/消息队列；教学班模型；AI 功能；前端页面实现（见 textbook-order-web / 小程序 PRD）。
- 各模块的异常分支、状态机、字段校验规则与错误文案在「四、功能说明」逐项定义，通用规范见模块 8。

### 二、功能列表

| 模块 | 功能名称 | 子任务 | 功能说明 | 优先级 |
|------|----------|--------|----------|--------|
| 认证 | 登录/改密 | JWT 签发/强制改密 | 账号密码登录签发 JWT；初始密码（学号/工号后 6 位）首登强制改密；失败 5 次锁 15 分钟 | P0 |
| 认证 | 账号管理 | 建号/停用/重置/导入建号 | 超管手动建号（含供货商）；Excel 全量导入即批量建号；停用不删除；openid 静默收集仅用于订阅消息 | P0 |
| 权限 | RBAC 与数据隔离 | 五表/CollegeScope | 权限码 `模块:业务:操作`；MyBatis-Plus 数据权限切面按角色注入过滤（ADMIN 全部/SECRETARY 本院/TEACHER·STUDENT 本人）；多角色并集 | P0 |
| 学期 | 学期与窗口引擎 | 自动开关/延长/提前截止 | 窗口起止任意设置；定时任务到点自动开/关；可无限次延长或提前截止后再延长；变更自动创建系统通知任务并审计 | P0 |
| 学期 | 双缓冲切换 | draft→active 原子切换 | 新学期数据导入 draft 区，校验后单事务切换激活，旧学期归档可查；同刻仅一个 active | P0 |
| 数据 | 组织与名单 | 组织三表/名单导入 | 学院/专业/行政班三级；学生/教师全量 Excel 异步导入（严格模式、幂等 upsert、消失者自动停用） | P0 |
| 业务 | 教师征订+两级审查 | 字段审查/内容审核 | 一人一学期一单；提交先过系统字段审查（白名单客观规则）再转超管内容审核；驳回解锁该表单补正重提 | P0 |
| 业务 | 学生选购 | 覆盖提交 | 窗口内一人一单，重提=覆盖；记录提交时学院/班级快照 | P0 |
| 业务 | 异动两级审批 | 字段审查/内容审核 | 教师/秘书提交学生或教师异动；系统字段审查→超管内容审核→通过才落库生效 | P1 |
| 通知 | 通知确认闭环 | 任务/重发/确认 | 通知任务（手动+系统自动）；未确认每日 1 轮订阅消息最多 5 轮；确认首次生效落库；历史表按学期归档；汇总导出 | P1 |
| 供货商 | 只读接口 | 清单/导出 | `/supplier-api/**` 独立模块物理隔离，仅 书名/ISBN/教师姓名/学院；一学院一 sheet；导出留审计 | P0 |
| 导出 | 导出中心 | 明细/汇总 | 教师征订明细（秘书本院/超管全院）、通知汇总、错误明细；后端文件流+一次性 token | P0 |
| 治理 | 审计日志 | 全操作留痕 | 登录/导出/账号操作/窗口变更/审批/复核全记录 | P0 |

> 各模块的进入/退出状态机与错误文案规范在「四、功能说明」逐项定义。

### 三、服务结构与接口导航

```
[类型：单体分层服务，按模块分包]
├─ auth/         → 登录、JWT、密码策略、强制改密拦截
├─ system/       → RBAC 五表、账号、组织三表、审计切面
├─ semester/     → 学期、窗口引擎、双缓冲切换
├── textbook/    → 教材库、课程、任课关系
├── order/       → 教师征订单（两级审查）、学生选购
├── approval/    → 异动两级审批
├── importexport/→ Excel 导入中心（异步批次）、导出中心
├── notify/      → 通知任务、订阅消息适配、确认追踪
├── supplier/    → 供货商只读接口（物理隔离，禁 import 学生 Mapper）
└── common/      → 统一响应、异常、审计注解、窗口校验注解
```

接口分组：`/api/auth/**`、`/api/admin/**`、`/api/secretary/**`、`/api/teacher/**`、`/api/student/**`、`/supplier-api/**`；JWT 过滤器统一鉴权，数据隔离由切面自动改写 SQL。各模块的异常分支、状态机、字段校验与错误文案见第四章；供货商模块红线：包内禁止 import 学生相关 Mapper（评审机检）。

### 四、功能说明

以下每个模块均包含功能描述、流程、异常分支、状态机、字段校验规范与错误文案；全局异常码与文案令牌见模块 8。

#### 模块 1：认证与账号

**功能描述**：签发/校验 JWT；账号生命周期（建号/停用/锁定/强制改密）。

**用户流程（登录）**
```
[POST /api/auth/login]
  ↓ 校验账号存在+状态正常+未锁定
  ├─ 密码校验通过 → 签发 JWT（含角色版本号）→ 返回 must_change_password
  └─ 失败 → 失败计数+1 → 满 5 次锁定 15 分钟 → 统一错误文案（防账号枚举）
```

**异常分支说明**：账号不存在/密码错误/已停用/已锁定返回同一文案「账号或密码不正确」（锁定态单独提示）；JWT 失效/停用即时失效（角色版本号比对）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 正常 | 建号/启用 | 停用/锁定 | 可登录 |
| 待改密 | 初始密码首登成功 | 改密成功 | 非 /me 与改密接口一律 403 |
| 锁定 | 失败 5 次 | 15 分钟 | 登录拒绝 |
| 停用 | 超管停用/导入比对停用 | 超管启用 | 登录拒绝，token 即时失效 |

**字段规范**

| 字段名 | 类型 | 必填 | 限制 | 校验规则 | 错误文案 |
|--------|------|------|------|----------|----------|
| userNo | string | 是 | ≤32 | 学号/工号格式 | 账号或密码不正确 |
| password | string | 是 | 6-64 | BCrypt 比对 | 账号或密码不正确 |
| newPassword | string | 是 | 8-64 | ≥8 位含字母和数字 | 新密码需 8 位以上且含字母和数字 |

**错误文案**：登录失败「账号或密码不正确」；锁定「账号已锁定，请稍后再试」；停用「账号已停用，请联系教材室」；未授权「登录已过期，请重新登录」。

#### 模块 2：RBAC 与数据隔离

**功能描述**：权限码鉴权 + 数据范围自动过滤。

**状态机：** 无显式状态；隔离切面按角色注入过滤条件（ADMIN 不过滤 / SECRETARY college_id / TEACHER·STUDENT user_id），多角色取并集。

**异常分支：** 越权访问资源（改 ID）→ 资源归属校验失败返回 403 并写审计；无权限码 → 403。

**字段规范：** 权限码格式 `模块:业务:操作`（如 `order:form:review`）；所有隔离查询列（college_id/user_id）必须建索引。

**错误文案：** 「无权访问该页面」/「无权执行该操作」。

#### 模块 3：学期与窗口引擎

**功能描述**：学期生命周期 + 征订窗口自动/手动控制 + 变更自动通知。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 学期 draft | 建学期 | 双缓冲切换 | 数据可导入不生效 |
| 学期 active | 切换（原子） | 归档 | 承载当前业务 |
| 学期 archived | 新学期激活 | — | 只读可查可导出 |
| 窗口 not_open | 学期 active 且未到 start | 到点自动开/手动开 | 填报/选购校验拒绝 |
| 窗口 open | start ≤ now ≤ end 且通道开 | 到点自动关/提前截止 | @WithinWindow 校验通过 |
| 窗口 closed | 超 end/提前截止 | 延长（无限次） | 校验拒绝 409 |

**异常分支：** 定时任务幂等（状态已变跳过）；切换与窗口变更加进程内锁+乐观校验防竞态；延长早于当前时间被校验拦截；每次变更写审计并自动创建系统通知任务（含新截止时间，范围=秘书+教师+学生）。

**字段规范：** window_start < window_end；通道开关独立于时间；切换请求携带预期版本号（乐观锁）。

**错误文案：** 「本期征订已截止」（409）、「征订尚未开始」、「存在更新的窗口配置，请刷新后重试」。

#### 模块 4：双缓冲切换与导入中心

**功能描述**：draft 学期全量导入→校验→原子切换；Excel 异步批次解析。

**用户流程**
```
[上传 .xlsx] → 落盘+批次落库 → @Async EasyExcel 流式解析 → progress_pct 轮询
  ├─ 行级校验失败 → 错误行收集（不中断）→ 完成后错误明细可下载
  └─ 全部通过 → 数据入 draft 区 → 人工确认 → 单事务切换 active（旧学期 archived）
```

**异常分支：** 文件魔数/类型/大小前置校验；解析中断批次标记 failed 可重传；幂等 upsert（user_no/ISBN 存在则更新）；导入期间 active 学期查询零影响（导入无感）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 批次解析中 | 上传成功 | 完成/失败 | progress_pct 递增 |
| 批次完成 | 解析完成 | — | ok/error 计数 |
| 批次失败 | 解析异常 | 重传 | 失败原因 |
| 学期 draft | 建学期 | 切换 | — |
| 学期 active | 原子切换 | 归档 | — |

**字段规范：** 上传仅 .xlsx ≤10MB；行级校验（必填/格式/外键存在）；严格模式不自动创建学院/班级；所有表含公共字段+reserve1~6 预留字段。

**错误文案：** 「仅支持 .xlsx 文件」「第 N 行：{校验错误}」「切换失败：存在更新的学期状态，请刷新」。

#### 模块 5：教师征订两级审查与学生选购

**功能描述**：征订核心业务；审查链=系统字段审查（自动白名单）→超管内容审核（理由必填）。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| draft | 创建 | 提交 | 可改 |
| field_check | 提交 | 审查完成 | — |
| rejected_auto | 字段审查失败 | 修复重提（窗口内不限次） | 错误逐字段返回 |
| pending_review | 字段审查通过 | 超管审结 | — |
| reviewed | 超管通过 | — | 计入汇总 |
| rejected | 超管驳回 | 补正重提（仅该表单解锁） | 理由落库 |

学生选购：draft → submitted（覆盖语义，重提=整单替换）→ closed 锁定；记录提交时 college/class 快照，异动不影响历史归属。

**异常分支：** 关窗瞬间提交 409；审查规则全为教师可控可修复的客观项（禁止"全驳回"死局）；并发提交以唯一约束+覆盖语义裁决；复核与补正并发以后端状态机裁决。

**字段规范：** 明细行=课程×班级×教材×数量（1 ≤ 数量 ≤ 班级人数上限）；教材必须在库；课程归属教师本人；驳回理由 1-200 字必填。

**错误文案：** 「存在 N 项问题，请按提示修复后重新提交」「本期征订已截止，提交未生效」「存在更新的表单状态，请刷新后重试」。

#### 模块 6：异动两级审批

**功能描述**：学生/教师异动申请的审批流，通过才落库。

**状态机：** pending_field_check → pending_review → approved（落库生效：更新 user 的 college_id/class_id）/ rejected（理由必填）。

**异常分支：** 字段审查（目标学号存在/目标班级存在/类型合法/前后值不同）失败自动驳回并逐项提示；审批通过落库与审计同事务。

**字段规范：** type ∈ {student, teacher}；payload_json 记录变更前后值；理由 1-200 字必填（驳回时）。

**错误文案：** 「目标学号不存在」「目标班级不存在」「变更内容无变化」。

#### 模块 7：通知确认闭环

**功能描述**：通知任务（手动+窗口变更自动）；未确认每日重发（订阅消息）最多 5 轮；确认首次生效。

**状态机**

| 状态名称 | 进入条件 | 退出条件 | 表现 |
|----------|----------|----------|------|
| 任务 active | 创建/窗口变更自动创建 | 5 轮完成/手动停止 | 重发扫描中 |
| 学生未确认 | 任务创建 | 点「收到」 | 每日重发目标 |
| 学生已确认 | confirm（confirmed_at 首次落库） | — | 移出重发名单 |
| 任务完结 | 达 round_limit | — | 停止重发 |

**异常分支：** 无授权/发送失败记 send_status=unauthorized/failed 如实落库（不做假"已送达"）；轮次扫描幂等；confirm 重复调用幂等。

**字段规范：** notice_record(task_id, student_id, round_no, sent_at, send_status, confirmed_at)；轮次上限与间隔为可配置校验参数（system_config）；确认请求无输入字段，服务端校验任务归属与幂等；索引 (task_id, student_id, round_no)。

**错误文案：** 发送状态枚举 sent/unauthorized/failed；确认接口成功返回 204。

#### 模块 8：供货商只读、导出与全局规范

**功能描述**：供货商物理隔离只读接口；导出中心；全局异常与错误文案令牌。

**异常分支（全局）**：参数校验失败 400（逐字段 message）；未认证 401；越权 403+审计；窗口冲突 409；状态冲突 409（提示刷新）；服务错误 5xx（不泄露堆栈）。

**状态机：** 供货商无业务状态；导出仅记录审计事件。

**字段规范：** 统一响应包络 `{code, message, data}`；分页参数 page/size（默认 1/20，上限 200）；供货商清单字段白名单=书名/ISBN/教师姓名/学院；导出文件流带一次性 token。

**错误文案（全局令牌）**：400「请求参数有误」；401「登录已过期，请重新登录」；403「无权执行该操作」；409「数据状态已变更，请刷新后重试」；500「服务开小差了，请稍后重试」。每次供货商导出写审计日志（谁/何时/范围）。

---

## 五、六大盲区自检清单（服务端适配版）

- [x] **接口结构**：模块分包+接口分组树明确，供货商物理隔离红线可机检。
- [x] **异常交互**：每个模块均含异常分支说明与全局异常码表（400/401/403/409/5xx）。
- [x] **状态机**：账号、学期、窗口、批次、表单、选购、审批、通知八类状态机完整。
- [x] **字段规范**：各模块关键字段+校验规则齐备；公共字段与 reserve1~6 预留字段规范统一。
- [x] **文案规范**：全局错误文案令牌 + 各模块专属文案。
- [x] **流程图**：登录/导入/审查/通知流程含成功/失败分支与出口。

## 六、安全设计自检清单

**账户安全**
- [x] **人机验证**：失败 5 次锁 15 分钟 + IP/账号双维度限频（V1）；撞库出现再引入验证码。
- [x] **频率限制**：登录/导出接口限频；全局网关限流。
- [x] **全局熔断**：Nginx + 应用层限流两级。

**内容安全**
- [x] **UGC 审核**：无公开 UGC；教师表单经超管内容审核后计入汇总（先审后汇总）。

**文件安全**
- [x] **文件类型限制**：仅 .xlsx，服务端魔数校验。
- [x] **文件大小限制**：≤10MB，超大分批。
- [x] **存储方案**：临时目录，导入后清理。
- [x] **防盗链**：导出一次性授权 token。

**AI 安全**
- [x] 无 AI 功能，不适用；字段审查/自动开关窗/重发轮次均为确定性规则，白名单化可审计。

**传输安全**
- [x] **HTTPS**：全站强制（moonzj.com 已备案）。
- [x] **敏感数据**：BCrypt；名单不含身份证字段（G1）；JWT 环境变量密钥。
- [x] **CORS**：同域反代，仅小程序跨端（合法域名白名单）。

**基础设施安全**
- [x] **环境变量**：DB/AppSecret/JWT 密钥注入，不硬编码。
- [x] **依赖审计**：定期依赖漏洞扫描。
- [x] **日志脱敏**：不含密码/token；审计仅记操作者与范围。

> 账号锁定/停用状态机见模块 1；全局错误文案令牌见模块 8；传输层异常（401/403）由全局异常码表统一处理。

自检通过："PRD 落地版已完成，六大盲区 + 安全设计 + AI 增强 8 大区块均已覆盖。"

---

## 七、问题校验（AI 增强）

- [x] **目标用户是否太泛**：服务对象=5 类角色前端 + 定时任务，边界清晰。
- [x] **MVP 是否过大**：P0 聚焦隔离/窗口/审查/导入导出；审批与通知闭环 P1；分库分表、多实例等移入停车场。
- [x] **需求是否单一**：只解决「征订业务统一后端」一件事。
- [x] **验证可行性**：Spring Boot 3 + MySQL 8 + EasyExcel 全为成熟栈；万行级在单机容量内（异步批次）；域名备案已完成。

## 八、输入设计（AI 增强 · 复用于数据输入）

| 输入项 | 来源 | 类型 | 是否必填 | 约束/校验规则 | 示例 |
|--------|------|------|----------|---------------|------|
| Excel 文件 | Web 上传 | 文件 | 是 | .xlsx ≤10MB；行级严格校验；幂等 upsert | 学生全量表.xlsx |
| 窗口配置 | 超管 | JSON | 是 | start<end；乐观锁版本号 | {"windowStart":"...","windowEnd":"..."} |
| 审核结论 | 超管 | JSON | 是 | action=pass 必填理由为空 / reject 理由 1-200 字必填 | {"action":"reject","reason":"..."} |
| JWT 凭据 | 前端请求头 | string | 是 | 签名+过期+角色版本号三重校验 | Authorization: Bearer ... |

- [x] 输入缺失给出兜底提示（逐字段 message）。
- [x] 输入异常（空/超长/格式错）处理路径定义（400+逐字段错误）。

## 九~十二、AI 输出设计 / AI Workflow / AI 职责拆解 / Badcase 分析

**本服务无 AI 功能，AI 输出设计、AI Workflow、AI 职责拆解不适用。** 全部自动判断（字段审查、窗口自动开关、重发轮次）为确定性规则代码，规则白名单化、可配置、可审计。若以 AI 职责动词描述：本服务不存在由模型执行的抽取/分类/匹配/排序/生成任务——最接近的自动判断（ISBN 匹配、错误行分类汇总、字段审查规则执行）均由确定性代码完成，无需失败兜底到人工以外的降级路径。

**Badcase 分析（≥8 个，系统边界/异常场景）：**

| 场景 | 可能原因 | 风险等级 | 应对策略 | 是否需要人审 |
|------|----------|----------|----------|--------------|
| [场景1-双缓冲切换与在途写冲突] | 切换瞬间有未完成提交 | 高 | 切换单事务+乐观锁；在途请求按旧学期快照完成 | 否 |
| [场景2-万行导入中途失败] | 超时/格式异常/内存 | 中 | 异步批次+failed 可重传+错误行收集不中断 | 是（错误行人工核对） |
| [场景3-窗口自动开关定时任务重复执行] | 任务重叠/重启 | 中 | 幂等设计（状态已变跳过）+进程内锁 | 否 |
| [场景4-越权改 ID 拉取别院数据] | IDOR 探测 | 高 | 数据隔离切面+资源归属校验+越权矩阵测试 | 否（测试必测） |
| [场景5-供货商接口探测学生字段] | 接口枚举 | 高 | /supplier-api 物理隔离无学生字段路径+审计 | 否（测试必测） |
| [场景6-同一教材被并发修改] | 教材库并发编辑 | 低 | 乐观锁+更新时间戳比对 | 否 |
| [场景7-订阅消息 token 失效批量发送失败] | access_token 过期竞态 | 中 | token 集中管理+刷新锁；failed 如实落库下轮重试 | 否 |
| [场景8-重复确认/重复重提风暴] | 前端重试/网络抖动 | 中 | confirm 与提交幂等（唯一约束+首次生效） | 否 |

## 十三、验证目标

| 验证项 | 方式 | 通过标准 |
|--------|------|----------|
| RBAC 与隔离 | 越权矩阵自动化用例 | 100% 拒绝+审计留痕 |
| 窗口引擎 | 自动开关/延长/提前截止演练 | 状态流转正确、通知自动创建 |
| 双缓冲 | 切换演练+在线会话并发 | 原子切换，业务无感 |
| 通知闭环 | 重发 5 轮+确认幂等 | notice_record 时间线完整 |
| 导入幂等 | 同文件重复上传 | 无重复数据 |
| 性能 | 万行导入/并发查询 | 批次进度正常，接口 P95 < 500ms |

## 十四、PRD 风险和下一步

**风险**
1. 订阅消息授权率不可控 → unauthorized 如实统计曝光未触达名单，线下兜底（R1）；
2. 单机定时任务多实例部署会重复执行 → 当前单实例部署，多实例时换分布式锁/xxl-job；
3. 学校重部署漂移 → 配置外置+初始化脚本+部署手册交付（R6）。

**下一步**
1. M1：工程骨架 + RBAC 五表 + JWT + 审计切面；接口契约冻结（与 web/小程序并行开发）；
2. M2-M4 按 03 号文档里程碑推进（窗口引擎/双缓冲/两级审查/通知闭环）；
3. M5：导出+越权测试+部署手册，moonzj.com HTTPS 试运行。
